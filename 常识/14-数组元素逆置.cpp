#include <iostream>
using namespace std;

int main(){
    
    // 数组元素逆置 元素倒序排列
    int num[5] = {1, 2, 3, 4, 5};
    // 数组个数
    int count = sizeof(num) / sizeof(num[0]);
    cout << count << endl;
    
    // 输出hello world c++
    cout << "hello world c++" << endl;
    system("pause");
    return 0;
}
